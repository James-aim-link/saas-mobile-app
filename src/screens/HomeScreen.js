
import React from 'react';
import { Box, Center, Heading, Text } from 'native-base';

const HomeScreen = () => {
    return (
        <Center flex={1} bg="white">
            <Heading>Workplace</Heading>
            <Text>Todo list and Calendar will appear here.</Text>
        </Center>
    );
};

export default HomeScreen;
